float nota1;
float nota2;

float media;

read(nota1);
read(nota2);

media = nota1 + nota2;
media = media / 2;

char aprovado;
aprovado = 'a';
char reprovado;
reprovado = 'f';

if (media >= 70.0) {
    write(aprovado);
} else {
    write(reprovado);
}